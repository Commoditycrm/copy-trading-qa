# Production smoke — READ-ONLY. NAMES ONLY. A separate written approval is required to run this suite.
# The prod suite must contain NO write/order/cancel calls. Only health, public schema, and read paths.
PROD_BASE_URL=https://kopyya.com
PROD_FRONTEND_URL=https://kopyya.com
# Optional read-only seeded account (scopes limited to reads). Provisioned + approved out-of-band.
PROD_SMOKE_TOKEN=__READ_ONLY_TOKEN_FROM_SECRET_STORE__
BROKER_MODE=none

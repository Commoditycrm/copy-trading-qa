# Shared QA env (test.kopyya.com) — NAMES ONLY. Real values come from the CI secret store, never the repo.
QA_BASE_URL=https://test.kopyya.com
QA_FRONTEND_URL=https://test.kopyya.com
BROKER_MODE=fake
# DB/Redis assertions only if a QA-only read path is provided; otherwise omit and assert via API.
DATABASE_URL=__QA_DB_URL_FROM_SECRET_STORE__
JWT_SECRET=__QA_JWT_SECRET_FROM_SECRET_STORE__
QA_ADMIN_EMAIL=__QA_ADMIN__
QA_ADMIN_PASSWORD=__FROM_SECRET_STORE__
